---
import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';

const metadata = {
  title: 'CinaGroup — AI 및 Web3 솔루션',
  description: 'CinaGroup 은 AI 검색 (CinaSeek), 지능형 로봇 (CinaClaw), 스킬 플랫폼 (CinaSkill), 블록체인 인프라 (CinaChain) 의 최첨단 솔루션을 제공합니다.',
};
---

<Layout metadata={metadata}>
  <Hero
    tagline="CinaGroup 에 오신 것을 환영합니다"
    title={'<span class="text-accent dark:text-white">AI 와 Web3</span> 의 미래를 구축'}
    subtitle="AI 검색, 지능형 로봇, 스킬 플랫폼, 블록체인 인프라의 최첨단 솔루션을 제공합니다. 전 세계 기업과 개발자를 지원합니다."
    actions={[
      {
        variant: 'primary',
        text: '제품 탐색',
        href: '#business',
        icon: 'tabler:arrow-down',
      },
      { 
        text: '문의하기', 
        href: '/ko/contact',
        icon: 'tabler:mail',
      },
    ]}
    image={{ 
      src: 'https://images.unsplash.com/photo-1677442136019-21780ecad995?ixlib=rb-4.0.3&auto=format&fit=crop&w=1740&q=80', 
      alt: 'AI 기술' 
    }}
  />

  <section id="business" class="py-16 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-4">
      <div class="text-center max-w-3xl mx-auto mb-12">
        <h2 class="text-3xl font-bold tracking-tight dark:text-white sm:text-4xl mb-4">
          사업 부문
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300">
          4 개의 강력한 플랫폼이 협력하여 포괄적인 AI 와 Web3 솔루션을 제공합니다
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {['CinaSeek', 'CinaClaw', 'CinaSkill', 'CinaChain'].map((name, i) => (
          <div class="group relative bg-white dark:bg-gray-800 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-200 dark:border-gray-700">
            <div class={`absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-${['green', 'orange', 'pink', 'purple'][i]}-400 to-${['green', 'orange', 'pink', 'purple'][i]}-600`}></div>
            <div class="p-6">
              <h3 class="text-xl font-bold dark:text-white mb-2">{name}</h3>
              <p class="text-gray-600 dark:text-gray-300 text-sm mb-4">
                {['지능형 검색', 'AI 어시스턴트', '스킬 플랫폼', '블록체인 네트워크'][i]}
              </p>
              <a href={`/ko/${name.toLowerCase()}`} class={`inline-flex items-center text-${['green', 'orange', 'pink', 'purple'][i]}-600 dark:text-${['green', 'orange', 'pink', 'purple'][i]}-400 font-medium text-sm`}>
                자세히 보기
                <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
